#include<conio.h>
#include<math.h>
#include<stdlib.h>
#include<stdio.h>
 int main()
{
	int modulo,num,cuadrado;
	printf("Introduce el numero: ");
	scanf("%d",&num);
	modulo=num%2;
	if(modulo==0)
	{
		printf("\n El numero es par");
		cuadrado=pow(num,2);
			printf("\nEl numero elevado al cuadrado es: %d ", cuadrado);
	}
	else
	{
		printf("\nEl numero es impar");
		elev=pow(num,3);
			printf("\n El numero elevado al cubo es: %d ",cuadrado);
	}
	printf("fin del programa");
	return 0;
	getch();
}
